<!--pages/detail/addrRemake/addrRemake.wxml-->
<view class='page'>
  <view class='midContent'>

    <view class='allHead allPirce'>
      <checkbox wx:if="{{!isShow}}" data-show="{{isShow}}" bindtap='checkAll'></checkbox>
      <checkbox wx:if="{{isShow}}" data-show="{{isShow}}" checked='true' bindtap='checkAll'></checkbox>
      <text class='allPe'>全选</text>
    </view>
    <checkbox-group bindchange="checkboxChange">
    <block wx:for="{{items}}" wx:key="id">
    <view class='goodsCont marginTop' >
      <view class='goodsCont_left'>
        <checkbox value='{{item.id}}' checked='{{item.checked}}'></checkbox>
        <view class='ib'>
        <view bindtap='navToGoods' class='navTGCont'>
        <text>NO.{{index + 1}}</text>
        <image class='avatarUrl' src='../../../images/mine/Category.png'></image>
        <text>入账金额</text>
        </view>
        </view>
      </view>
      <view>
      </view> 
    </view>

    <view  class='goodsCont'>
        <text>名字</text>
        <text>12345678980</text>
    </view>   
    
     <view class='goodsCont  fStart'>
      合计：<text class='red'>￥1</text>
    </view>
    </block>
    </checkbox-group>

  </view>
</view>

  <view  class='pos_bot'>
    <button bindtap='saveRemake'>确认</button>
  </view>
